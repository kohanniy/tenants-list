import { Stack } from '@mui/material';
import { styled } from '@mui/material/styles';

export const WrapperStyled = styled(Stack)(({ theme }) => ({
  justifyContent: 'space-between',
  alignItems: 'center',
}));
